# Essential dependencies for Gemini Geo-Compliance Detection System
# Core dependencies - all pre-compiled
fastapi==0.115.0
uvicorn==0.32.0
pydantic==2.8.0
python-dotenv==1.0.1
httpx==0.28.0
python-multipart==0.0.9

# Gemini AI - lightweight and reliable
google-generativeai==0.3.0

# Note: LangGraph and LangChain dependencies are commented out due to version conflicts
# If you need these features, install them separately with compatible versions
# langgraph==0.2.0
# langchain==0.2.0
# langchain-google-genai==2.1.10
# langchain-community==0.2.0

# Vector store and embeddings (optional - can be installed separately if needed)
# chromadb==0.4.0
# sentence-transformers==2.2.2

